{
  "name": "newsmapper2",
  "version": "2.0.0",
  "description": "NewsMapper 2.0 — Natural resources news via GDELT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist -r https://github.com/MetoliusResearch/newsmapper.git -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
